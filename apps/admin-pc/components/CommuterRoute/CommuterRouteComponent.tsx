import React from 'react';

import CommuterRouteContainer from '../../containers/CommuterRouteContainer';

const CommuterRoute = (props) => <CommuterRouteContainer {...props} />;

export default CommuterRoute;
