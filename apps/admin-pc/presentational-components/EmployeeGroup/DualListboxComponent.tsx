import React from 'react';

import EmpGroupDualListContainer, {
  OwnProps,
} from '../../containers/EmpGroupDualListContainer';

const DualListbox = (props: OwnProps) => (
  <EmpGroupDualListContainer {...props} />
);

export default DualListbox;
