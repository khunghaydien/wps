import { LegalAgreementRequestList } from '../../../../domain/models/LegalAgreementRequest';

export const defaultValue: LegalAgreementRequestList = {
  requests: [],
  availableRequestTypes: ['Monthly'],
};
