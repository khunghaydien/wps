import React from 'react';

import ExpTypeLinkConfigContainer from '../../containers/ExpTypeLinkConfigContainer';

const ExpTypeLinkConfigComponent = (props) => (
  <ExpTypeLinkConfigContainer {...props} />
);

export default ExpTypeLinkConfigComponent;
