import React from 'react';

import ExpLinkExpGridContainer from '../../containers/ExpLinkExpGridContainer';

const ExpenseTypeGridComponent = (props) => (
  <ExpLinkExpGridContainer {...props} />
);

export default ExpenseTypeGridComponent;
