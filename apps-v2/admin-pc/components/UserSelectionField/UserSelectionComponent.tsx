import React from 'react';

import UserSelectionContainer from '../../containers/UserSelectionContainer';

const UserSelection = (props) => <UserSelectionContainer {...props} />;

export default UserSelection;
